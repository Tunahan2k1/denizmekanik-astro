---
// 1. KOD ALANI (Component Script) - Burası BOŞ DEĞİL!
// Bu alan sunucuda çalışır ve tarayıcıya gönderilmez.

// Burada, carousel'de göstereceğimiz resimlerin ve alt metinlerinin
// listesini bir JavaScript dizisi (array) olarak hazırlıyoruz.
const slides = [
  {
    src: "/assets/images/anasayfa/slider6.webp",
    alt: "Deniz Mekanik üretim tesisi"
  },
  {
    src: "/assets/images/anasayfa/slider1.webp",
    alt: "Deniz Mekanik portal vinç üretim hattı"
  },
  {
    src: "/assets/images/anasayfa/slider2.webp",
    alt: "Alçak tavan vinci kurulumu ve testi"
  },
  // ... diğer slaytlar buraya eklenebilir
];
---
<!-- 2. HTML ŞABLONU (Component Template) - Burası da BOŞ DEĞİL! -->
<!-- Bu alan, yukarıdaki veriler kullanılarak oluşturulur ve tarayıcıya gönderilir. -->

<div id="mainCarousel" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    {/* 
      İşte sihirli kısım: Yukarıda hazırladığımız 'slides' dizisini burada bir
      döngüye sokuyoruz (.map() fonksiyonu ile) ve her bir eleman için 
      otomatik olarak bir <div> ve <img> oluşturuyoruz.
    */}
    {slides.map((slide, index) => (
      <div class={`carousel-item ${index === 0 ? 'active' : ''}`}>
        <img
          src={slide.src}
          class="d-block w-100"
          alt={slide.alt}
          loading={index === 0 ? 'eager' : 'lazy'}
        />
      </div>
    ))}
  </div>
  <button
    class="carousel-control-prev"
    type="button"
    data-target="#mainCarousel"
    data-slide="prev"
  >
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </button>
  <button
    class="carousel-control-next"
    type="button"
    data-target="#mainCarousel"
    data-slide="next"
  >
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </button>
</div>